# Swift / SwiftPM
.build/
.swiftpm/
resources/images/

# macOS
.DS_Store

# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
*.dSYM
*.dSYM.zip
# Auto-generated workspace directory inside .xcodeproj (not needed for SPM-only projects)
ios/VisualWinelistIOS.xcodeproj/project.xcworkspace/

# pre-commit
.pre-commit-cache/

# Docker runtime data (mounted volumes)
image-cache/
data/

# Coverage reports
.coverage
.coverage.*
coverage/
htmlcov/

# Python (backend — uv, pytest, ruff, mypy)
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
# NOTE: uv.lock is intentionally committed — do not ignore it.

# Node / web (Vite + React + TypeScript)
node_modules/
web/dist/
.vite/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# NOTE: package-lock.json is intentionally committed — do not ignore it.

# Local environment variables
.env
.gstack/
